# RKC_RPG_TABLE

No portable behavior from this DLL is needed yet. Its first SF_EXE port will
add a static target and `rkc_rpg_table.hpp`, using
`src/reconstructed/RKC_RPG_TABLE` as the
behavioral reference.
