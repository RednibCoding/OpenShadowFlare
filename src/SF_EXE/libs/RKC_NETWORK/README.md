# RKC_NETWORK

No portable behavior from this DLL is needed yet. Its first SF_EXE port will
add a static target and `rkc_network.hpp`, using
`src/reconstructed/RKC_NETWORK` as the
behavioral reference.
